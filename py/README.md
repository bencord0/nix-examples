# Nix example with python

### Run a python script

Dependencies are installed in `./shell.nix`

Run the python script under `nix-shell`.

    $ nix-shell --run ./main.py

This workflow can replace virtualenvs.
